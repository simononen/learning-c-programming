#include <stdio.h>

int main() {

    int x, y;

    x = 7;
    y = 5;
    printf("Given x = %d, y = %d\n", x, y);
    printf("x / y produces: %d\n", x / y);
    printf("(float)x / (float)y produces: %f\n", (float)x / (float)y);

    return 0;
}